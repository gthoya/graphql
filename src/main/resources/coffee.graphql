schema {
    query: Query
    mutation: Mutation
}

type Coffee {
    cid: Long
    name: String
}

type Query {
    coffees: [Coffee]
    coffee(id: Long): Coffee
}

type Mutation {
    modifyCoffee(id: Long, name: String): Coffee
    addCoffee(name: String): Coffee
}
